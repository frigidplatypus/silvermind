<!-- SPECKIT START -->
Active spec: `specs/003-js-native-backend/plan.md` — strip sbtask, JS-only backend.

For additional context about technologies to be used, project structure,
shell commands, and other important information, read the current plan at
`specs/003-js-native-backend/plan.md`.

For Android build setup, see `~/.local/share/opencode/plans/003-android-support/plan.md`.

Work inside the Nix dev shell unless explicitly told otherwise. Use `nix develop`
from the repo root before running builds, tests, or git commands. The flake's
shellHook enables the repository hooks from `.githooks` automatically.
<!-- SPECKIT END -->
